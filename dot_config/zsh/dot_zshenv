### put in home directory if you wish to use .config/zsh as source directory

export XDG_CONFIG_HOME="$HOME/.config"
export ZDOTDIR="$XDG_CONFIG_HOME/zsh"

if type "nvim" > /dev/null; then
  export EDITOR="nvim"
elif type "vim" > /dev/null; then
  export EDITOR="vim"
fi
