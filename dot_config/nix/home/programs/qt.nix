{...}: {
  ### only applies when starting an application from the cli (e.g. dolphin)
  qt = {
    enable = true;
    platformTheme = "kde";
    style.name = "breeze";
    # platformTheme = "gtk3";
    # style.name = "adwaita-dark";
  };
}
