{...}: {
  ### theme set for things like dolphin (e.g. dmenu, hyprland comamand)
  ### (does not apply if running dolphin from cli)
  qt = {
    enable = true;
    platformTheme = "kde";
    style = "breeze";
  };
}
