# generate config file
spicetify

# spicetify init
spicetify backup apply enable-devtools

# apply config
spicetify update

# reload in spotify app
Ctrl Shift R / Command Shift R

### if all else fails, reinstall???
