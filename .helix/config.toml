[editor.file-picker]
### allow file picker to see .gitignore files like .chezmoidata.toml
git-ignore = false
### if I ever do silly symlink things, though I shouldn't need to
deduplicate-links = false
