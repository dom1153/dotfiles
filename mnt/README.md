This placeholder directory holds symlinks to mounted directories.

Such as auto-mounted nfs directories, samba shares, or maybe external drives.

Formally they may get mounted elsewhere, but I don't want to memorize those paths.
